# V1 implementation observations

Starting revision: `bc9e98d3ed99fc040c341f50f74e6b9cd017d8dc`; accepted V0 package source hash: `77ef233f9541072dbac0140d11dd84dac1a3494c60fb324b74413a71b25c0400`. Work began 8 September 2026. The worktree was clean; no applicable AGENTS.md was found. V0 artifacts/configuration/notes, ledger and retained keys are fingerprinted for unchanged-content checks; their historical files will not be rewritten.

V1 is limited to evaluator correction, protocol-preserving text optimization, gated/arithmetic coders and a bounded pilot. Its ledger will be separate from V0's 3,163.653124 charged seconds, with at most 7,200 additional model-process seconds. No V2 work is authorized.

Before any V1 output inspection, choose I1 (gradient), I5 (PCG64 noise), T1 (32 UTF-8 bytes) and T3 (64 UTF-8 bytes, accented text), the existing prompt/row, and fixed/gated/arithmetic methods: 12 pilot units. Calibrate from two ordinary traces per modality with seeds 4101/4102 (text, 1,024 tokens each) and 4201/4202 (image, full visible canvas). Thresholds will freeze before stego generation; no outcomes will tune them. These small traces are pilot calibration, not full V1 qualification.

The sandbox command launcher still fails before launch at bwrap loopback setup; approved tool escalation is used. No existing environment access control is disabled.

## Evaluator, profiling and CPU method foundation

- Corrected final allocation validation and separated progress mode. Twenty-two tests passed; read-only revalidation of the public V0 ten-case tree returned allocation_complete=true, attempted/completed/exact=10, all_recovered=true. Historical artifacts/results were not rewritten.
- Profile job v1-001 charged 168.4820 seconds. Its 616-token replay took 152.5767 seconds with cProfile enabled: filtering 139.3971; native full-prefix detokenization 82.403; tokenization 39.784; stable ordering 12.499 seconds (overlapping cumulative categories).
- The inspected llama-cpp-python 0.3.23 native detokenizer concatenates token pieces, except for an initial BOS space adjustment. Carrier BOS is forbidden. Cache exact token pieces and the current carrier bytes only; full-prefix UTF-8/tokenization checks remain. Pin the inspected _internals.py SHA-256 44f3db6523465479d850b5c80f196b02123621e9e89a5826a61e425bb1e2be4f. Exact top-256 cutoff ties and float64 normalization are preserved; validations no longer sort the vocabulary.
- Job v1-002 charged 374.9620 seconds. On one new immutable I1 packet, reference encode/decode took 136.7024/136.7251 seconds; optimized took 43.0349/42.4707. All 616 state fingerprints (eligible IDs, raw probability bytes, ordered IDs) and final UTF-8 bytes matched; both packet replays matched. These are diagnostic runs, not extra independent pilot observations.
- Thirty-two CPU tests now pass, including all small interval bounds at 3/4/5-bit test precision against a fractional half-interval oracle, enumerated finite messages, explicit 32-bit/rounding/zero-step/suffix vectors, gate boundary/skips, and optimization ties/probability/sampling equivalence.
- Arithmetic is independently written from the published algorithm and the approved A1 contract. Inspected NeuralSteganography arithmetic.py revision 14e982564aeaf9a33f7b4de440deda2184d17f12; no source copied because reuse permission remains unresolved. No additional methodological amendment is presently needed.
- Calibration's first controller invocation failed at import before any model launch; added the explicit project-root import path and reran the same predeclared traces. No GPU job or stego attempt was charged for that CPU launcher failure.
- Predeclare controls now: one new ordinary text trace (seed 4301) to the maximum realized pilot text length, with saved matched prefixes; one full PNG control (seed 4302). These are shared controls, linked explicitly rather than counted as independent copies. Calibration traces remain separate.

- A new synthetic saved-file integration test initially assumed every random packet must terminate under an alternating non-dyadic distribution; one trial exhausted capacity. CPU reproduction (PCG64 seed 4, 292 bytes) reached 2,334 bits and an interval of width three straddling 1/2. The specified rounding/overflow rule reduces uniform-32 to one bin there, so no common bits can emerge. An explicit fractional-oracle boundary vector confirms this is A1 capacity stagnation, not incorrect bit recovery. The success-path test now uses a dyadic uniform distribution; stagnation remains a separate failing-capacity test. No model fixture, packet, threshold, coder or pilot configuration was changed. The pilot launch followed the failed test due to a missing shell orchestration guard; its first fixed/gated cases were already verified independently. Subsequent work treats test exits explicitly.

- The midpoint stopped after six units: five exact recoveries and one text arithmetic capacity failure, with sender/receiver agreeing on 1,450 packet bits at 2,048 tokens. Its conservative projection was 7,535.736 seconds: it reserved 25% on spent time as well as future work. Keeping every future cap-envelope estimate unchanged but reserving 25% only on future work gives 6,881.937 seconds within the same 7,200-second hard cap. A small continuation helper starts only the wholly unattempted I5/T3 pairs, in a new key/run namespace. Their unused original packet preparations remain private and are explicitly superseded; no previously generated output is retried, no seed/threshold/fixture changes, and no independent count is added.
- The exact pilot package source d70e5e3321e1e5003eaa7687ca867229892ada937d89a70660bb7e3665aed577 is preserved in artifacts/v1_review/pilot_source with its hash manifest.

- Both runs finished all twelve preselected units: ten exact recoveries, two image-to-text arithmetic capacity failures. The two arithmetic failures have matched sender/receiver bit counts (1,450 and 771 at the 2,048-token cap), mean observed surprisal 0.70868 and 0.37655 bits/token, and mean quantization L1 approximately 7.28e-6 and 6.14e-6. These traces support low conditional information capacity as the main limitation; they are distinct from the synthetic tiny-interval stagnation example. No fixture was replaced or attempted again.
- After all stego jobs exited, tightened evaluator checks for duplicate case names even with distinct work IDs, actual staged profiles, GPU model identities and retained-carrier hashes. A terminal receiver backend failure remains a complete failed allocation, not a successful recovery or unstarted case. Added matched-prefix time/score snapshots to ordinary control reporting; coder/backends/receiver code did not change. Forty CPU tests passed (7.164 seconds). The exact measured pilot source snapshot remains available.
- Final read-only V0 revalidation: expected/attempted/completed/exact 10, allocation_complete=true, all_recovered=true. Content hashes match all 113 protected evidence/configuration/ledger files and 24 retained private key files. Historical V0 cost remains 3,163.653124 seconds.
- The two predeclared controls passed the prospective gate: spent 4,230.500 seconds plus bounded future costs and a 25% future reserve remain below 7,200. Control generation includes matched text prefix timing/score checkpoints, with no extra neural scoring pass. Controls are shared samples, not twelve independent ordinary observations.

- All GPU work stopped after job v1-032. V1 charged 4,690.205214 seconds (1.302835 hours); 2,509.794786 seconds remain in its initial allocation. V0+V1 cumulative cost is 7,853.858338 seconds (2.181627 hours). All 32 model processes have process-specific GPU activity; sampled framebuffer memory is retained separately from true allocator-peak claims.
- Final CPU suite: 41 tests passed, 7.224 seconds in the packaged run. Three additional actual CLI checks passed: partial ten-case allocation exits 1, strict historical V0 exits 0, strict V1 exits 1. Public V1 validation with --allow-failures exits 0, with 12 attempted, 10 complete/exact and two capacity failures.
- Final measured projection: remaining V1 qualification 10.18 additional hours (12.73 with reserve), with explicit unmeasured static-mask and allocation assumptions. The one-context main stego generation/replay alone projects to 45.04 hours. Including development, shared per-payload/context controls and one 25% reserve gives 88.32 hours (160.58 with two contexts), above the 40-hour ceiling. No larger allocation was launched.
- V1 is not fully qualified. The arithmetic text successes, full development matrix/corpora/second-context work and a feasible study budget remain unresolved. No V2, commits, pushes, new environments, dependency installations or weight downloads occurred in this turn.
