# V1 qualification implementation observations

Accepted starting source: b5f2d1e3b9e901e5dfe6a181b6ca5d95dc35ac4b. Existing V0/V1/V1.1/V1.2 evidence is read-only. Private preservation hashes are retained in .runtime/v1_qualification/baseline.json.

Preparation (CPU only):
- Reconciled 18 accepted stego credits and six ordinary/control credits against the frozen allocation: 134 stego units, 12 ordinary audits, 46 controls and 20 lossless receiver jobs remain.
- Verified original authenticated packets against frozen source bytes for I1/I5/I6 and T1/T3/T6 groups. I5 and T3 use the second pilot packet run, not the unused first-run packets. Existing groups are never re-encrypted.
- Added the V1-only absolute 50,400-second allowance amendment; applying it again is a no-op. V0 remains 7,200 seconds. Neither ledger was reset.
- The existing pilot entry point now supports one frozen qualification queue. Immutable atomic terminal records are authoritative; append-only events can be recovered after a crash between completion and event append. Valid saved carriers resume at a fresh receiver, not a new sender. Invalid/incomplete senders stop for inspection rather than rerolling.
- Optional receiver bit/interval audit files are output-only under ignored .runtime. The separate evaluator checks recovered prefixes for incomplete gated/arithmetic packets after receiver exit. These are not extra model passes or observations. Receiver inputs remain exactly carrier/profile/context/key.
- Preserved the coding, packet, text/image backend and probability modules byte-for-byte. Full text offload and strict image CUDA checks remain enforced. New ordinary traces audit, never change, frozen thresholds.
- Frozen lossless selection rule: T1–T10, row1 and row2, fixed method. Rewrite RGB PNG with compression level 9 and no ancillary metadata, verify pixels on CPU, then use the original profile/context/key in a fresh receiver.
- All 60 CPU tests passed in 11.592 seconds, including eight focused continuation/budget tests. Accepted public evidence revalidated without regeneration: V0 10/10 exact; V1 10 exact + two capacity failures; V1.2 five exact + one static-drift failure.
- Initial measured forecast: remaining qualification 34,602.564 seconds before reserve, inclusive of 20 PNG replays; 43,253.205 seconds with 25% headroom fits the 44,174.673-second V1 balance. Whole project, including history, 120 prospective stego units, 40 shared controls and one reserve: 29.6994 hours. This is conditional, not permission for V2.
- Two CPU preparation mistakes were corrected before GPU work: a missing expected-kind argument in an inspection check and a literal backslash-n in the new atomic JSON writer. Tests cover the corrected writer. Neither affected historical artifacts or model execution.
- During execution, added a CPU-only finalizer and two focused namespace/public-coverage regression tests. These files are not imported by model jobs and are not part of the pre-execution package hash. The finalizer records its own hash and separately verifies terminal/event identity, control-prefix hashes, original-packet bindings, public copies, protected history and budget totals. No historical job is attributed to this reporting code.
- CPU inspection of the retained new arithmetic audits found transient single-bin/non-narrowing steps in two successful PNG cases: T3/row2 has two consecutive steps at interval width 2; T7/row2 has seven steps (maximum consecutive run six) at widths 2 and 25. Each had 256 eligible values quantized to one bin, then resumed progress and recovered exactly. All independent interval/partition checks passed. These are observed finite-precision effects of the unchanged A1 contract, not implementation corrections. The six new arithmetic text capacity cases show no unchanged intervals or single-bin collapse, consistent with the accepted diagnosis of the original two failures. No additional GPU replay was launched for this CPU inspection.

