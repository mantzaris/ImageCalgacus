# Captions and interpretation

## Complete text-to-photograph transport

The first frozen BSDS500 test identifier, 2018, carries its assigned literal 63-byte UTF-8 message. Both text panels show the entire source/recovered message, with line wrapping for display only. The center image is the actual saved 256 × 256 RGB8 model-rank PNG. A fresh GPU receiver uses only the carrier, declared profile and key to reconstruct the invariant coarse context and rank map. Authentication and independent source-byte comparison pass. The displayed photograph is not enhanced or regenerated. This is main Figure 2 in the integrated draft.

## Canonical covers, delivered carriers and differences

The first three frozen test identifiers, 2018, 3063 and 5096, selected before embedding outcomes. Each row shows the 256 × 256 RGB8 canonical cover, delivered model-rank PNG and absolute RGB channel difference multiplied by 32. Black denotes zero difference; the sparse modifications remain intentionally faint at the fixed amplification. Nearest-neighbor display enlargement does not alter the carrier files. Fidelity is relative to the canonical crop, not the original JPEG container. These examples illustrate pixel preservation, not a human-observer or steganalysis result. This is companion Figure S7.

## Paired results table

Six development and twenty held-out cover/text groups, each with model-rank and simple parity arms using identical sealed packets and positions. Exact recovery requires authenticated parsing followed by a separate comparison with the source. PSNR uses all RGB channel errors and peak 255. SSIM averages three valid-center Gaussian channel scores, window 11, sigma 1.5, population covariance, K1 = 0.01 and K2 = 0.03. Means are descriptive and pairs are not independent extra payloads. Encode/decode means are charged fresh-process wall times including startup, loading, graph setup, serialization and teardown. Parity is model-free. The theoretical 53.06845 dB worst-case bound is separate from measured PSNR.
